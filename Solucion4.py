from random import *

def generaNumeroAleatorio(minimo, maximo):

    if minimo > maximo:
        aux = maximo
        minimo = maximo
        maximo = aux
        return ran int(minimo, maximo)
    if minimo < maximo:
        return ran int(minimo, maximo)